name: Main Workflow

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  call-hello:
    uses: ./.github/workflows/job-no-1.yml

  call-quynh:
    uses: ./.github/workflows/job-no-1.yml
